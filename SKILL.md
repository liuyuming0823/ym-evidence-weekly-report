---
name: ym-evidence-weekly-report
slug: ym-evidence-weekly-report
display_name: 周报证据整理
display_name_en: Evidence Weekly Report
displayName: 周报证据整理
description: "把零散工作记录整理成每条都带来源的周报，区分已完成与计划，不编造成果。当用户说「写周报」「整理这周工作」「本周总结」「周报生成」时使用。 也适用于「整理周报」「工作周报」「weekly report」这类说法。"
description_zh: "基于工作记录生成可追溯的周报，成果均标注来源，不虚构业绩"
description_en: "Turn scattered work notes into a traceable weekly report with sourced claims"
summary: "基于工作记录生成可追溯的周报，成果均标注来源，不虚构业绩"
category: office-efficiency
version: 0.1.0
author: 刘玉明
tags: [写周报, 整理周报, 本周总结, 工作周报, 周报生成]
trigger:
  - 写周报
  - 整理周报
  - 本周总结
  - 工作周报
  - 周报生成
  - weekly report
agent_created: true
---

# 周报证据整理 (ym-evidence-weekly-report)

把零散工作记录整理成周报，核心是**可追溯**而不是「写得好听」。每条成果都标出它来自哪条记录，计划不会被写成已完成，没有证据的内容单独列出来让你确认。

## 何时使用

- 用户说「写周报」「整理这周工作」「本周总结」「周报生成」「weekly report」时
- 用户贴了一堆提交记录、待办清单、聊天摘录、日报，说「把这些整理一下」时
- 用户抱怨「周报不知道写什么」「这周好像什么都没干」时

## 运行前提

- 依赖：无第三方依赖，纯文本处理
- 不读取任何本地文件，除非用户明确给出路径
- 不联网、不上传任何数据

## 输入契约

| 项 | 必需 | 说明 |
|---|---|---|
| 工作记录 | 必需 | 一段或多条自由文本，可带日期，可来自待办清单、提交记录、聊天摘录、日报 |
| 汇报周期 | 可选 | 默认最近 7 天 |
| 汇报对象 | 可选 | 主管 / 团队 / 客户；默认直属主管，语气偏正式 |
| 是否允许推断 | 可选 | 默认允许推断，但所有推断必须进「需要你确认」清单 |

**缺失处理：**

- 缺周期 → 按最近 7 天处理，并在报告开头注明「周期未指定，按最近 7 天整理」
- 缺汇报对象 → 默认直属主管，语气偏正式
- **记录为空 → 直接说明无法生成，列出需要提供的信息，绝不编造内容**

## 执行步骤

1. **建立证据索引** — 给每条记录编号（记录 1、记录 2…），保留用户原始措辞的要点。这一步必须最先做，后面所有结论都要能回溯到编号。

2. **逐条判定状态** — 每条记录四选一，判定规则见 `references/status-rules.md`：
   - 已完成：有明确结果证据
   - 进行中：有动作无结果
   - 阻塞：有阻碍描述
   - 计划：仅表达意图

3. **归档到四段** — 只有「已完成」能进「本周完成」；「计划」只能进「下周计划」。这是本技能最容易被破坏的约束，务必逐条检查。

4. **标注来源** — 每条完成项末尾附来源编号，形如 `[记录 3]`。不允许出现无来源编号的成果句。

5. **列出推断** — 凡从记录推导、但没有直接证据支撑的内容（例如「这个功能应该能提升转化率」），单独列进「需要你确认的推断」清单，不混进正文。

6. **自检** — 输出前逐条核对：完成段每条是否都有来源编号？下周计划有没有混进本周完成？有没有美化措辞改变事实？

## 输出格式

Markdown，套用 `templates/report-template.md`，四段 + 来源索引 + 待确认清单，控制在 800 字以内。

```
## 本周完成
- 完成 XX 模块的开发与自测 [记录 3]

## 进行中
- 正在排查 XX 接口超时问题 [记录 5]

## 阻塞与需支持
- XX 环境权限未开通，需运维协助 [记录 7]

## 下周计划
- 推进 XX 模块联调

## 需要你确认的推断
- [记录 2] 只提到「调通了接口」，「性能提升 30%」是我推断的，请确认是否有实测数据
```

## 能力边界

- 不代替用户判断绩效，不生成述职、OKR 评分
- 不自动发送邮件或消息
- 不美化措辞改变事实，不把推测写成结论
- 默认不跨次存储；只有用户明确要求时，才把周报追加到 `history/` 便于下周对比

## 常见坑

1. **把计划写成已完成** — 用户记录里写「准备下周重构 XX 模块」，模型容易把它当成已做。判定依据是「有没有结果证据」，不是「有没有提到这件事」。
2. **来源编号张冠李戴** — 记录多了以后容易标错编号。做法是编号和要点在第一步就固定下来，后面只用编号引用，不重新理解原文。
3. **推断混进正文** — 「应该」「预计」「大概」这类词出现时，说明这是推断，必须挪到待确认清单。
4. **空输入硬编内容** — 用户只说「帮我写周报」没给记录时，不能为了输出结果而编造。正确做法是停下来说清楚需要什么。
5. **记录无日期时乱猜周期** — 应该在报告里标注「用户未提供日期，按输入顺序编号」，而不是自行假设都在本周内。

## 验收用例

- 正常：10 条带日期记录 → 四段齐全，完成项均带来源编号
- 边界 1：记录全是「计划」语气 → 完成段为空，并说明「未找到可确认的完成项」
- 边界 2：记录无日期 → 按输入顺序编号，周期处标注「用户未提供日期」
- 失败：空输入 → 明确拒绝生成，列出需要提供的信息

## 目录说明

- `references/` — 状态判定规则与示例
- `templates/` — 周报输出模板
- `scripts/` — 预留（本技能当前为纯文本处理，无需脚本）
